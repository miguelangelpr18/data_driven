// Feature flags del sitio.
//
// SHOW_PLANS: sección de "Planes" en pausa (draft) mientras se redefine el
// manejo de precios. El código de la sección sigue en el repo, solo no se
// renderiza. Para reactivarla, cambiar a `true` — vuelven la sección y sus
// enlaces en el nav (header y footer) automáticamente.
export const SHOW_PLANS = false;
